# Server Port
server.port=9000

# Application Name
spring.application.name=auth-server

# 2026 Best Practice: Detailed Security Handshake Logging
# This combines both your YAML sections into a clean list
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2.server.authorization=DEBUG

# Metadata & Identity Configuration
# Set this to ensure the server knows its own identity
# spring.security.oauth2.authorizationserver.issuer=http://localhost:9000